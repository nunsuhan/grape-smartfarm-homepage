FROM node:18-alpine

WORKDIR /app

# 의존성 파일 복사 및 설치
COPY package.json package-lock.json ./
RUN npm ci --only=production

# 애플리케이션 코드 복사
COPY . .

# Next.js 빌드
RUN npm run build

# 포트 노출
EXPOSE 3000

# 개발 서버 실행 (빌드된 앱 실행)
CMD ["npm", "start"]